# KnownMap 当前下一步

更新时间：2026-09-01

## 已完成执行切片：视觉设计系统阶段 0

依据：`doc/design/v1/11-visual-design-system.md` 第 10 节、`D-V1-031`。

已完成 `tokens.json → tokens.css` 生成脚本、对比度校准、共享 token 产物、四端消费入口、样式漂移检查和根 `check` 接入；插件版本从 `1.2.2` 升至 `1.2.3`。历史页面已有的 352 个裸色值暂存于 `tools/style-check-baseline.json`，不在本轮混入页面视觉重做。

验证：根测试 138 项、v1 测试 344 项、`npm run check`、插件类型检查、本地/生产构建通过。

下一步：阶段 1 先建立教师端 5 页与管理端 2 视图的截图基线，再审计并收敛 shared `base.css` / `supplement.css`，逐步删除对应历史裸色值。

## 已完成执行切片：本地公开试用申请

本轮已按 `D-V1-027` 完成本地表单替换：销售页只提供入口，点击后进入独立申请页面；申请页无需登录提交，课程类别为必填，B 站课程链接为选填并做简单主机名校验；提交后显示成功结果，不发送邮件；服务端使用限流和隐藏字段防刷；管理员端查看申请并维护“待联系 / 已联系 / 已关闭”。当前开发阶段没有飞书历史数据迁移，也不自动创建教师账号。

验证已完成：后端 58 项、根测试 132 项、v1 测试 335 项、接口/模块/文档门禁、管理员端类型检查与构建、教师端构建；浏览器已验证公开提交、管理员查看和状态更新。

下一步：若进入发布准备，先按发布环境重新设置管理员凭证、生产 CORS 和限流策略，再执行发布前完整门禁；本轮浏览器产生的本地测试申请仅用于开发验收。

## 当前执行切片：学习伙伴弹窗与猫咪资源压缩

依据：`D-V1-023`、`D-V1-026`；设计：`docs/superpowers/specs/2026-08-30-student-companion-package-settings-design.md`；计划：`docs/superpowers/plans/2026-08-30-companion-popup-and-asset-compression.md`。

当前步骤：先用失败测试冻结 cat-v1 的 WebP/Ogg 资源路径、同一 popup 内的学习伙伴页面和五段声音试听。
验证：`npm --prefix v1 test -- extension/content/companion-assets.test.ts extension/manifest/targets.test.ts extension/popup/popup.test.ts extension/settings/settings.test.ts`

不变量：不改课程包 schema、不改后端或教师节点文件、不混入当前工作树已有的教师端修改和数据库备份；本轮不实现任何远程角色包下载。

## 当前执行切片：课程互动节点课程化编辑引导

依据：D-V1-025；设计：docs/superpowers/specs/2026-08-29-course-node-content-guidance-design.md；计划：docs/superpowers/plans/2026-08-29-course-node-content-guidance.md。

当前步骤：D-V1-025 实现与验收已完成；后端全库格式门禁仍有 3 个既有未格式化文件，待作为独立维护项处理。
验证：教师端 38 个测试文件 / 310 项通过；v1 类型检查与构建、插件类型检查与 local/production 构建、契约检查、根测试与工程门禁通过；后端 pytest 42 项通过，`ruff format --check .` 仅报告 `asset_storage.py`、`routes.py`、`schemas.py`。

不变量：不增加节点字段，不修改课程包 schema，不修改插件行为；companion 资源不纳入本轮。

然后检查：
git status --short --branch
git diff --name-only

预期：只看到既有用户修改；不得覆盖任何 v1/web/teacher 计划外修改，也不得加入、移动或删除 companion 资源。

### D-V1-025 已完成：课程互动节点课程化编辑引导（2026-08-30）

已在隔离 worktree `codex/course-node-content-guidance` 完成方案 A：只改教师端字段引导、占位、提示、默认标题和预览文案，保留 `title`、`content`、`interactionData`、`presentationHints` 以及 `保存节点` / `保存草稿` 两步边界；以本轮实现基线 `e533417..9d454be` 核对，`v1/contracts/`、`v1/extension/`、后端 schema、API 和学生端行为均未修改（更早的 D-V1-024 插件测试变更不属于本轮）。

自动化证据：`npm test` 通过（根测试 130 项 + v1 测试 310 项）；`npm run check`、secret scan、dependency check、契约检查通过；插件 type-check 与 `build:all` 通过；v1 type-check/build 通过；后端 `ruff check .` 通过、pytest 42 项通过。后端 `ruff format --check .` 仍报告 3 个未被本功能修改的既有文件，未为本功能顺带格式化。

实际页面证据见 `tests/manual/v1/course-node-content-guidance-20260830.md`：隔离 QA 环境中四种节点均可填写和预览，选择题 4 项完整展示，保存后刷新可恢复，375px 页面无横向溢出。

下一步：如需仓库所有门禁全绿，另立维护提交处理后端上述 3 个 Ruff 格式问题；继续开发本功能时从 `docs/superpowers/handoffs/2026-08-29-course-node-content-guidance-continuation.md` 的收口记录开始，不要重做已通过的教师端实现和测试。

## 当前执行切片：学生插件课程库、课程升级与设置

本轮依据 `D-V1-022` 和已确认设计
`docs/superpowers/specs/2026-08-28-student-course-library-and-upgrade-design.md`，
执行计划为
`docs/superpowers/plans/2026-08-28-student-course-library-and-upgrade.md`。

开始编码前必须先完成三项文档门禁：在 06 号接口清单登记四个新学生端点；在
`v1/contracts/versions.json`、TypeScript 版本定义和对应测试中登记 HTTP API、
插件消息与插件存储的 2.2.0 兼容版本；在追踪矩阵中保留现有需求编号并随实现阶段
回填设计、实现和自动化证据。本轮不把字幕加入课程包，遵守 `D-V1-014`。

交付顺序：先冻结存储/消息/HTTP 契约和版本策略，再实现版本元数据与状态迁移，
随后分别完成后端资源授权、插件资源缓存、升级队列持久化、调度恢复和 UI。每一步
先写失败测试并运行对应的跨层检查；资源、队列和原子提交失败时必须保持最后有效课程。

## 最近已完成：B 站课程视频引用与分 P 精确匹配

本轮已接受 `D-V1-021`：课程绑定对象从单独 BVID 改为 `platform + video_id + page + cid?`。
`p` 是从 1 开始的分 P，`cid` 已知时是更强身份；`vd_source` 及其它来源追踪参数不参与匹配。
设计记录见 `docs/superpowers/specs/2026-08-28-bilibili-video-reference-design.md`，执行计划见
`docs/superpowers/plans/2026-08-28-bilibili-video-reference-plan.md`。

本轮交付顺序：先用失败测试锁定 URL 规范化和精确匹配，再修改后端、课程包、插件存储与运行时，最后运行跨层门禁。
无精确候选时，插件不得显示陪伴 UI；同 BVID 不同分 P、CID 不完整和 SPA 切换都必须有负向证据。

本轮本地代码与测试已完成；云端尚未切换到这次新迁移和新课程包，待提交并按发布流程部署后再做线上验证。

## 当前阶段

旧后端、旧教师 Web 和根契约副本均已删除。当前代码、静态站点、品牌资源与契约
统一位于 `v1/`；后端具备独立依赖、单一空库迁移、41 个设计端点和完整自动化闭环。

2026-08-25 已完成教师课节编辑器参考样式对齐的第一轮实现：当前课节菜单、长视频时间段、
共享节点图标、横向时间轴、字幕上下文和节点属性弹窗已接入；本轮补齐无草稿课节加载、
5/10/30 分钟分段选择和与节点对齐的竖向刻度线；实现计划见
`docs/superpowers/plans/2026-08-25-teacher-editor-reference-parity.md`。

2026-08-26 已完成互动节点结构化编辑与持久化：正文统一保存为版本化 `RichPageDocument`，题型数据与窗口提示分离，
节点类型下拉项复用图标和颜色；旧版 C 方案字段、普通正文回退和 HTML 真源不再保留。课程包 v3 增加节点媒体资源清单，
B 站播放引用仍与节点图片、音频、视频完全分开。

本次验收按产品负责人要求只以测试结果为准，不设置 7 日观察期，不迁移生产数据，
也不等待真实用户交付。

2026-08-26 已接受初期发布约定 `D-V1-013`，唯一入口为 `tools/release.sh`。

2026-08-26 已按 `D-V1-014` 完成教师侧字幕持久化：字幕随草稿保存并恢复，发布快照冻结字幕，教师课程文件支持
导入预览、确认导入和导出；课程包 v3 与插件边界保持不变。

2026-08-27 已完成教师登录后课程总览：课程按草稿、已发布、已归档分组，已发布课程显示互动节点、已发授权和已领取设备
聚合；课程列表接口已预留 `metrics.student_submission_count`，当前返回 `null`，前端明确标记为当前未接入，遵守学生回答只保存在本机的边界。

2026-08-27 已完成教师端媒体资源输入：图片、音频和视频可上传或通过 URL 由服务器导入，草稿与发布快照只保存服务器生成的
`assetId` 和资源元数据；教师编辑器不再要求手工填写资源 ID。学生插件按 `assetId` 下载和本机保存仍属于后续 R1–R10 切片。

2026-08-27 已完成教师节点预览与代码契约同步：预览消费节点级 `presentationHints` 的大小、位置和样式，实时渲染结构化正文及节点媒体，且与插件运行时共用展示参数解析规则；`保存节点` 与 `保存草稿` 的两步保存边界保持不变。

2026-08-29 已完成 `D-V1-024`：互动节点窗口支持 10%–66% 的连续宽高、0%–100% 的连续中心点位置；教师端支持滑块、拖动和方向键微调，后端提供节点展示配置局部更新端口，学生插件和课程升级链路同步读取新格式；当前待在真实浏览器中完成窄屏、全屏和内容超限人工验收。

2026-08-27 已完成已发布课程版本操作的教师端界面：课程卡片底部提供“修改本版本”“增加版本”“授权码管理”；
前两项使用一句话确认状态变化后调用原子版本草稿端点，后一项进入按 `course_id` 管理授权码的独立页面，
支持单个和 1–100 个批量生成、完整码展示、领取设备摘要、首次/最近领取时间和终止授权。

## 下一步

### 已确认但暂不开发：课程版本升级与授权码兼容

依据 `D-V1-018`，不同发布版本按不同课程身份管理；新版本可在后续建立课程族和课程升级关系。后续需要支持：

- 教师选择原课程版本可升级到的新课程版本；
- 同一课程族的旧授权码默认看到新版本课程并更新课程；
- 学生确认跨版本更新，以及本机学习状态的兼容迁移；
- 旧授权码、课程版本、授权范围和更新资格的兼容校验。

本条只作为后续开发记录，本阶段不修改代码、不新增接口、不迁移现有课程或学生本机数据。

### `D-V1-018`/`D-V1-019` 当前开发切片

产品负责人已批准新增后端业务端点。本轮交付：

- 后端提供原子的“修改本版本/增加版本”端点，前端不使用导出、导入、归档组合业务事务；
- “修改本版本”不保留当前发布并退回草稿，“增加版本”保留源发布并创建新 `course_id` 草稿；
- 已发布课程卡片与草稿卡片同尺寸和基本布局，固定提供“修改本版本”“增加版本”“授权码管理”；
- 授权码管理页支持单个和 1–100 个批量生成，显示完整码、状态、领取设备数、首次及最近领取时间；
- 完整码不明文落库，由后端仅向所属教师安全重建；授权仍绑定 `course_id` 并自动解析最新可交付版本。

教师端页面已接入上述端点：版本操作完成后进入返回结果对应的草稿课程，授权码管理使用 `?access=<course_id>`
保存页面位置，返回课程总览不会改变课程数据。

跨独立课程升级关系、旧授权码跨课程兼容和学生本机学习状态迁移仍不开发。

当前发布口径已调整：发布不要求所有课节完成草稿或测试预览，只检查课程至少有一节课节；课程详情页和教师工作台草稿卡片
均提供“发布课程”入口。后续若恢复更严格的内容完整性校验，需要另立需求并同步发布契约。

实现本地公开试用申请闭环：

1. 新增 `TrialApplication` 本地申请对象和 Alembic 迁移；当前没有飞书历史数据需要迁移；
2. 新增无需登录的公开提交接口，字段包含称呼、联系方式、课程类别、视频状态、教学问题、字幕情况，以及选填 B 站课程链接和产品验证问题；
3. B 站链接只做 `bilibili.com`/`b23.tv` 简单格式校验；提交成功只显示成功结果，不发送邮件或其它通知；
4. 增加限流和轻量隐藏字段防刷；失败、限流或结果未知时不得显示成功；
5. 管理员后台增加申请查看和“待联系 / 已联系 / 已关闭”状态管理，申请提交不自动创建教师账号；
6. 完成公开提交、权限隔离、重复提交、保存失败、保留/清理和人工浏览器验收。

本轮不接入飞书 API、不迁移历史申请、不建设 CRM、不开放注册。

开发前：

1. 读 `doc/lessons.md`；
2. 在 v1 需求与设计中确认需求和边界；
3. 把本轮可检查交付物写在本文件；
4. 实现并测试后更新 `changelog.md` 与必要文档。

## 当前验证入口

```bash
cd v1/backend
uv sync --frozen
uv run ruff check .
uv run ruff format --check .
uv run pytest

cd ../..
npm test
npm run check
npm --prefix v1 run type-check
npm --prefix v1 run build
```
