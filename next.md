# KnownMap 当前下一步

更新时间：2026-08-22

## 当前执行切片：v1 实现阶段 0（工程基线与干净初始化）

- 当前分支：`codex/v1-rewrite`；重构基线：`main@0e503a4`。
- v1 需求目录 01–13、开放项 `OPEN-01` 至 `OPEN-08` 和旧资料 `SRC-001` 至 `SRC-082`
  已完成人工审核；`D-V1-010` 修订后重新冻结为 `1.0.2`。
- v1 设计 01–09 已全部通过人工审核并冻结为 `1.0.x`；`D-V1-011`、`D-V1-012` 已确定
  聚合 JSON 策略和「从干净初始化开始、不迁移旧数据」。
- 开发计划、代码重构执行计划和测试计划已建立。
- 文档收口已完成：编号引用全部可解析、44 份旧文档已归档、`doc/INDEX.md` 的当前权威
  只剩 v1 真源、`doc/dev-rules.md` 已建立、256 个稳定编号已进入追踪矩阵。
  四项文档检查已进入 `node --test`，不再依赖人工记得。
- v1 HTTP 端点清单已建立（06 第 4.5 节，41 个端点按六模块分组），
  `node tools/endpoint-check.mjs` 与后端实现持续对照：14 个已对齐、7 个旧路径待退役、
  20 个待实现。未登记端点会导致测试失败。
- 模块表归属已建立（03 第 7.0 节，12 张表分配到六模块），
  `node tools/module-check.mjs` 检查跨模块表访问：9 处已知越界待阶段 1 修复，
  新增越界一律失败。

## 阶段 0 六组工程任务的真实状态

| 任务 | 状态 |
| --- | --- |
| 目录、职责边界、版本清单 | 待执行 |
| 课程包、插件消息、HTTP 契约（`ARCH-DEC-02` JSON Schema/OpenAPI 真源） | 部分完成：HTTP 端点清单已建立并进入门禁；JSON Schema 真源待建立 |
| 空库 migration、seed、初始化检查 | 部分完成：隔离空库测试已完成，v1 初始化实现待建立 |
| 插件本机存储 Schema、身份和证明接口 | 待执行 |
| 匿名课程测试夹具 | 待执行 |
| 秘密、依赖、契约版本和文档检查 | 部分完成：文档检查已建立，秘密/依赖/契约版本检查待建立 |

阶段 0 总门禁尚未通过。

## 当前工作顺序

1. 建立共享契约目录和版本清单，把课程包与插件消息提升为版本化 JSON Schema
   （HTTP 端点清单已完成，JSON Schema 真源是 `ARCH-DEC-02` 剩余部分）；
2. 建立匿名课程夹具，覆盖重复课节、同视频多课节、多发布版本和多授权来源；
3. 建立插件新的 `storage_schema_version`、`LocalIdentity` 和本机证明存储接口；
4. 补齐秘密扫描、依赖锁和契约版本检查，与文档检查一起进入同一门禁；
5. 通过阶段 0 门禁后进入阶段 1：服务端身份、工作空间和课程领域。

每个阶段完成后同步需求、设计、代码、测试、追踪矩阵、lessons、changelog，提交并推送。
实现中发现与冻结需求/设计的真实冲突时，先暂停实现并回写决策或设计，不用代码偷偷改范围。

## 当前人工决策边界

- 阶段 0 当前没有待确认的产品或范围决策。目录、版本清单、契约骨架、空库检查、匿名夹具和
  自动化检查均按已冻结需求/设计直接执行；阶段 0 结束时只汇总结果，不逐文件请求人工审核；
- 只有实现发现会改变冻结需求/设计的真实冲突，或需要新增兼容承诺、改变用户可见流程时，
  才集中提交人工决策；
- 不需要逐项确认：失效链接、状态漂移、重复说明、命名和编号一致性、已被需求或代码事实
  唯一决定的内容；
- 每次最多集中提交 5 项真正的产品或工程取舍。

## 待处理的遗留事项

- `doc/英文面试问答流程...srt`（`SRC-054`）权利未核验。按 `D-V1-008`，取得权利证据或
  替换为可证明授权/自制的匿名素材前，不得进入任何发布物；建立课程夹具时不要引用它。
- `backend/app/` 仍是按技术分层的平铺结构，与设计 03 第 7 节的六个业务模块边界不一致。
  表归属已在 03 第 7.0 节定义，9 处已知越界登记在 `tools/module-check.mjs` 的
  `KNOWN_VIOLATIONS` 并附修法，属阶段 1 修复范围。新增越界会导致测试失败。
  重构为 `modules/<domain>/` 的时机见代码重构执行计划。

## 阶段入口与历史

- 文档总索引：`doc/INDEX.md`；
- 项目开发规则：`doc/dev-rules.md`；
- 需求真源：`doc/requirements/v1/README.md`；
- 设计真源：`doc/design/v1/README.md`；
- 上一阶段完整执行记录：`doc/archive/2026-08-22-pre-v1-design/next.md`。
