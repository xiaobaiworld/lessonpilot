# KnownMap 当前下一步

更新时间：2026-08-27

## 当前阶段

旧后端、旧教师 Web 和根契约副本均已删除。当前代码、静态站点、品牌资源与契约
统一位于 `v1/`；后端具备独立依赖、单一空库迁移、41 个设计端点和完整自动化闭环。

2026-08-25 已完成教师课节编辑器参考样式对齐的第一轮实现：当前课节菜单、长视频时间段、
共享节点图标、横向时间轴、字幕上下文和节点属性弹窗已接入；本轮补齐无草稿课节加载、
5/10/30 分钟分段选择和与节点对齐的竖向刻度线；实现计划见
`docs/superpowers/plans/2026-08-25-teacher-editor-reference-parity.md`。

2026-08-26 已完成互动节点结构化编辑与持久化：正文统一保存为版本化 `RichPageDocument`，题型数据与窗口提示分离，
节点类型下拉项复用图标和颜色；旧版 C 方案字段、普通正文回退和 HTML 真源不再保留。课程包 v3 增加节点媒体资源清单，
B 站播放引用仍与节点图片、音频、视频完全分开。

本次验收按产品负责人要求只以测试结果为准，不设置 7 日观察期，不迁移生产数据，
也不等待真实用户交付。

2026-08-26 已接受初期发布约定 `D-V1-013`，唯一入口为 `tools/release.sh`。

2026-08-26 已按 `D-V1-014` 完成教师侧字幕持久化：字幕随草稿保存并恢复，发布快照冻结字幕，教师课程文件支持
导入预览、确认导入和导出；课程包 v3 与插件边界保持不变。

2026-08-27 已完成教师登录后课程总览：课程按草稿、已发布、已归档分组，已发布课程显示互动节点、已发授权和已领取设备
聚合；课程列表接口已预留 `metrics.student_submission_count`，当前返回 `null`，前端明确标记为当前未接入，遵守学生回答只保存在本机的边界。

2026-08-27 已完成教师端媒体资源输入：图片、音频和视频可上传或通过 URL 由服务器导入，草稿与发布快照只保存服务器生成的
`assetId` 和资源元数据；教师编辑器不再要求手工填写资源 ID。学生插件按 `assetId` 下载和本机保存仍属于后续 R1–R10 切片。

2026-08-27 已完成教师节点预览与代码契约同步：预览消费节点级 `presentationHints` 的大小、位置和样式，实时渲染结构化正文及节点媒体，且与插件运行时共用展示参数解析规则；`保存节点` 与 `保存草稿` 的两步保存边界保持不变。

## 下一步

### 已确认但暂不开发：课程版本升级与授权码兼容

依据 `D-V1-018`，不同发布版本按不同课程身份管理；新版本可在后续建立课程族和课程升级关系。后续需要支持：

- 教师选择原课程版本可升级到的新课程版本；
- 同一课程族的旧授权码默认看到新版本课程并更新课程；
- 学生确认跨版本更新，以及本机学习状态的兼容迁移；
- 旧授权码、课程版本、授权范围和更新资格的兼容校验。

本条只作为后续开发记录，本阶段不修改代码、不新增接口、不迁移现有课程或学生本机数据。

完成公开飞书试用申请的品牌化改造：

1. 生成并保存 KnownMap 品牌背景图；
2. 把飞书表单切换为更聚焦的布局，同步标题、说明和题目顺序；
3. 同步销售页试用入口文案，保持飞书仍是申请记录真源；
4. 重新发布并用公开链接复验外观、匿名访问和入口一致性。

本轮不自建表单后端、不改变字段的数据边界、不把提交申请写成自动创建账号。

开发前：

1. 读 `doc/lessons.md`；
2. 在 v1 需求与设计中确认需求和边界；
3. 把本轮可检查交付物写在本文件；
4. 实现并测试后更新 `changelog.md` 与必要文档。

## 当前验证入口

```bash
cd v1/backend
uv sync --frozen
uv run ruff check .
uv run ruff format --check .
uv run pytest

cd ../..
npm test
npm run check
npm --prefix v1 run type-check
npm --prefix v1 run build
```
