# 教师平台本地闭环人工验收

状态：节点 8 核心功能已由用户确认基本可行；以下完整边界验收仍待逐项执行。

2026-08-20 基本可行性证据：真实 Chrome 已能显示 B 站页面书包；本轮定位并修复旧 service worker 空响应导致的永久 loading，新增工具栏学生入口、当前课程和教师登录入口。用户确认“当前功能基本可行”。此确认不替代下表对无效码、不同课程、跨 BVID、SPA 和日志的逐项证据。

## 准备

1. 按 `backend/README.md` 启动本地 FastAPI，并准备一门已发布课程和一个有效授权码。
2. 在 `chrome://extensions` 开启开发者模式，加载仓库 `src/` 目录；每次代码更新后点击 KnownMap 的“重新加载”，再刷新 B 站页面。
3. 打开课程绑定的 B 站视频页，同时打开扩展 service worker、页面和 FastAPI 日志。

不得把完整授权码、Cookie、节点正文或学生答案粘贴到本文件。

## 节点 8 验收表

| 步骤 | 预期 | 实际 | 证据 |
| --- | --- | --- | --- |
| 输入有效授权码 | “课程”区域新增一条记录，显示完整可点击课程 URL 与“打开课程视频” | 待执行 | 待补 |
| 刷新目标视频 | 已安装课程仍可读取，UI 只出现一份 | 待执行 | 待补 |
| 播放跨过第一个节点 | 视频暂停并显示教师配置的互动 | 待执行 | 待补 |
| 输入无效授权码 | 显示可操作错误，原课程和进度不变 | 待执行 | 待补 |
| 输入另一门课程授权码并取消 | 不替换原课程或学习状态 | 待执行 | 待补 |
| 打开其它 BVID | 不挂载课程小助手，不残留旧互动 UI | 待执行 | 待补 |
| B 站 SPA 来回切换 | 每次最多一份监听器和 UI | 待执行 | 待补 |
| 检查三处日志 | 无授权码原文、Cookie、节点正文和答案 | 待执行 | 待补 |

节点 9 将在本表下继续记录从空数据库开始的完整教师发布到学生互动证据。
