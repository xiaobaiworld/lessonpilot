# 教师编辑器收尾浏览器验收

日期：2026-08-20

环境：Codex 内置浏览器，`http://localhost:4173/teacher-web/editor.html#timeline`；FastAPI `127.0.0.1:8000`；本机 SQLite 已升级至 `0007_access_code_types`。

## 验收结果

- [x] 使用 `teacher-test-01` 登录后加载既有课程和授权码历史。
- [x] 无字幕时课节时长、最后刻度和结束文案均为 `08:33`。
- [x] 轴线右端和结束文案中心均为 845px；结束点没有提前或延后。
- [x] 右下快捷区为 fixed 定位，包含“打开视频”“发布课程”“完成”。
- [x] 初始历史读取 2 条长期授权码；其中 1 条来自已经丢失的旧临时密钥，无法再验证。完整备份数据库后移除该测试记录。
- [x] 创建 1 条短期授权码后，最终总数为 2，分类数为短期 1、长期 1；点击分类只显示对应记录，明细含尾号、创建时间、有效期和状态。
- [x] 本次创建的短期授权码格式正确，公开课程下载返回 HTTP 200。
- [x] 点击“完成”后草稿状态为“草稿已保存”，路由回到 `#home`；重新进入编辑器后仍显示 `08:33` 和最终 2 条有效授权记录。

## 安全检查

- 列表只展示末五位提示，不返回授权码原文或摘要。
- 完整授权码仅在本次创建区域显示并用于一次下载验证。
- `.env` 和 SQLite 数据库均被 Git 忽略。
- 清理前数据库可从 `backend/knownmap-before-invalid-code-cleanup-20260820.db` 恢复；该备份同样被 Git 忽略。
