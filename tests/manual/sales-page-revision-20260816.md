# 销售页与试用入口修订 人工验证记录

对应计划：`doc/plans/stage-1b-sales-page-revision.md` 第 7 节
页面：`teacher-web/forsales.html`
日期：2026-08-16

状态：**自动化与可自动化的浏览器检查已完成；第 3、4 项待你在本机执行**

## 自动化检查

`node --test tests/*.test.js` → 193 pass / 0 fail。

| 计划要求 | 结果 |
| --- | --- |
| 品牌主张固定在主标题上方 | 通过（连位置一起锁：排到 h1 之后就不再是主张） |
| 首屏自成一体（身份、承诺、目标视频、协助说明） | 通过 |
| 唯一明确的主 CTA | 通过（`tests/sales-page-copy.test.js` 断言 `.cta-primary` 恰好 1 个） |
| 不包含「发给我们」 | 通过 |
| 含开发者身份、B 站/微信联系说明 | 通过 |
| 含安全边界说明 | 通过（同时禁掉「绝对安全」「安全审计」等 6 个夸大说法） |
| 四种正式节点，无「老师补充」 | 通过（浏览器实测下拉 4 项、组件条 4 个按钮） |
| 学习结果明确为示例、未完成开发 | 通过 |
| 不把插件、报告、多学生数据写成已上线 | 通过 |
| clipboard 成功与失败提示都含粘贴位置 | 通过（两条提示都断言含「私信\|微信」） |
| 飞书链接不存在时无死链接 | 通过（表单入口整块不显示，见下） |

## 浏览器实测

服务：`python3 -m http.server 4173`，Chrome headless。

| 项 | 结果 |
| --- | --- |
| 1440 / 900 / 375px 横向溢出（人工检查第 5 项） | 三档均无元素逃出视口。见 `tests/manual/sales-page-responsive.html` |
| 375px 主 CTA 与复制按钮 | 宽 309px、高 46px，都在视口内，无遮挡 |
| 字幕栏行数（COURSE-06 未被本次改动破坏） | 8 节点 × 三档视口全部 ≥ 7 行。见 `tests/manual/subtitle-rail-lines.html` |
| 页面自身控制台错误 | 无。B 站播放器 iframe 自身的网络日志与页面错误无关 |

## 待你在本机执行

计划第 7 节的人工检查里，有两项自动化替代不了：

3. 滚到 CTA，确认只需回复私信或填写表单、不要求注册；
4. 点击复制话术，确认文本真的进入系统剪贴板且提示明确。

第 4 项 headless 环境没有系统剪贴板，`navigator.clipboard` 在非安全上下文里也会走失败分支，
所以「真的进了剪贴板」只能在你的浏览器里确认。两条提示文案已由自动化锁定。

## 本次未做的部分及原因

- **飞书极简表单入口（计划第 3 节）未实现。** 计划第 24 行与第 102 行都规定：
  URL 未提供时先不显示表单入口，禁止写不可用的占位 URL。目前库里没有这个 URL，
  所以页面只有私信主 CTA。测试已加断言：一旦页面提到表单却没有真实飞书 URL 就失败。
- 提供 URL 后要做的事：加次 CTA 按钮「填写 1 分钟试用信息」、旁文
  「不方便私信？留下课程情况，我会联系你。」、`target="_blank"`，然后删掉该断言的豁免分支。

## 2026-08-19 飞书表单补充实施

- 已创建 `KnownMap 真实课程互动改造申请` 表单草稿，并按 D-012、D-013 配置基础联系字段和未来方向验证问题；完整定义见 `doc/trial-intake-form-design.md`。
- 已新增独立模块 `teacher-web/trial-intake.js`，销售页只保留次 CTA 挂载点；无效或缺失 URL 时继续失败关闭。
- 已把模块纳入 GitHub Pages 与生产发布白名单，并补充自动化测试。
- 公开地址：`https://my.feishu.cn/share/base/form/shrcnGpoiVzLw8v5sD5K2TV8sFb`。
- 匿名验收：无飞书 Cookie 的干净 Chromium 访问返回 HTTP 200；标题、8 个问题、必填标识、固定选项和提交按钮均可见，没有登录前置页。为避免污染真实线索，本次未创建测试提交。
