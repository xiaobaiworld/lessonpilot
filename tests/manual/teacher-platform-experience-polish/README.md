# KnownMap 互动课程工具体验校正验收

日期：2026-08-18

分支：`codex/course-design-platform-polish`

## 验收范围

- 登录密码显示或隐藏、登录中状态和登录成功后清理；
- 未登录导航、我的课程、课程资料和课程设计页；
- 无字幕状态、真实 SRT 导入和节点字幕引用重绑；
- 点击、拖放和键盘节点放置；
- 草稿保存、发布锁和授权码会话隔离；
- 1440 x 900、900 x 1000、375 x 812 响应式；
- console error、page error 和页面级横向溢出。

## 自动化结果

```text
Node: 234 pass / 0 fail
Backend pytest: 37 pass / 0 fail
Bandit: no findings
pip-audit: no known vulnerabilities
JavaScript syntax: pass
git diff --check: pass
```

## 浏览器结果

三个视口均通过：

- 登录页密码默认隐藏，可切换显示和隐藏；
- 登录成功后密码输入值清空并恢复为 `password`；
- 页面无横向溢出、console error 或 page error；
- 未导入字幕时字幕栏为空，时间轴仍保持固定视频完整 3:42；
- 已保存的 5 个节点位置保持不变；
- 字幕选择按钮可键盘聚焦；
- 节点组件选中后焦点进入时间轴，方向键、Home、End 和 Enter 可完成放置；
- 导入真实 SRT 后，保存请求中的节点全部携带当前字幕引用；
- 模拟延迟发布期间，保存按钮、节点组件区和时间轴保持锁定；
- 注销后授权码面板隐藏，授权码值、复制状态、发布版本、课程、字幕和 `#timeline` 路由清空。

最终专项浏览器测试对发布与授权响应使用本地拦截，避免继续污染本地版本和授权码数据；登录、课程读取、字幕解析和草稿保存仍走真实本地服务。测试凭据和授权码原文未写入仓库或本记录。

## 独立审查

独立只读对抗审查提出 8 项问题：

- 7 项确认有效并修复：跨账号授权码残留、字幕重绑未同步草稿、键盘核心路径不可达、无字幕时长过短、发布竞态、登录后密码残留、运行时内部文案；
- 1 项属于当前阶段既定固定 BVID 边界，保留限制并把错误提示改为明确说明。

## 回退点

- 设计和计划基线：`0ebc521`
- 功能实现：`d34d8a0`

主分支未在本任务中修改。
