# KnownMap v1 阶段性临时总结

文档编号：`TEMP-V1-001`

日期：2026-08-22

状态：临时进度快照，不替代需求、设计或开发计划真源

## 1. 当前结论

需求整理和 v1 设计整理已经完成，文档收口已执行，当前已进入 v1 实现阶段 0：工程基线与干净初始化。

产品代码尚未进入 v1 正式重写；当前代码仍是 v0.9.1 原型基线，只用于现状证据、经验提取和兼容拒绝测试。

## 2. 已完成

- v1 需求目录、开放项和旧来源资料已完成审核与归类。
- v1 设计文档 01–09 已完成审核并冻结。
- 已确定 v1 不迁移无价值的旧服务端业务数据和学生本机数据（`D-V1-012`）。
- 已建立 v1 开发计划、代码重构执行计划和测试计划。
- 已完成一次隔离空数据库初始化测试：新数据库执行迁移后只建立结构，不带入旧业务记录。
- 已完成文档收口（`02-legacy-document-register.md` 第 10.3 节第 4–6 步）：
  - 删除需求条目里的 `关联要求`/`关联需求` 字段共 204 处，其中 306 处引用指向从未签发的
    编号族；上下游关系改由追踪矩阵单向维护；
  - 44 份旧文档以 `git mv` 归档到 `doc/archive/2026-08-22-pre-v1-rewrite/`，历史可追溯；
  - 重写 `doc/INDEX.md` 与根 `README.md` 的权威关系，新建 `doc/dev-rules.md`；
  - 建立 `doc/traceability/v1-requirements.tsv`，覆盖 256 个稳定编号；
  - 四项文档检查（编号可解析、链接、矩阵覆盖、权威唯一）进入 `node --test` 门禁。
- 工作区旧数据库仍作为只读历史证据保留，没有执行物理删除。

## 2.1 测试基线的准确含义

Node 自动化测试 341 项通过（原 331 项 + 文档一致性 10 项）；后端 pytest 97 项通过。

**这不是 v1 覆盖率。** 原 331 项针对 v0.9.1 原型：销售页文案、教师时间线、授权码面板、
插件下载流程等。它们证明当前原型没有回归，不证明任何 v1 需求已实现。
v1 测试证据按 `doc/plans/v1-test-plan.md` 逐阶段建立，人工验收写入 `tests/manual/v1/`。

新增的 10 项文档一致性测试是唯一与 v1 直接相关的自动化证据，它们检查文档体系本身的
一致性，不检查产品行为。

## 3. 阶段 0 明细

阶段 0 共六组工程任务。目前严格状态为“两组部分完成、四组待执行”：

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| 目录、职责边界、版本清单 | 待执行 | 尚未建立 v1 共享目录和版本清单入口 |
| 课程包、插件消息、HTTP 契约 | 待执行 | 现有契约仍是旧原型契约，尚未建立 `ARCH-DEC-02` 要求的 JSON Schema/OpenAPI 真源 |
| 空库 migration、seed、初始化检查 | 部分完成 | 隔离空库测试已完成，新的 v1 初始化实现仍待建立 |
| 插件本机存储 Schema、身份和证明接口 | 待执行 | 只完成设计，尚未进入 v1 代码 |
| 匿名课程测试夹具 | 待执行 | 还未建立覆盖重复课节、同视频多课节、多发布和多授权的夹具 |
| 秘密、依赖、契约版本和文档检查 | 部分完成 | 文档检查已建立并进入门禁；秘密扫描、依赖锁和契约版本检查待建立 |

因此，阶段 0 尚未通过总门禁。

## 3.1 文档收口暴露的欠账

文档收口前，需求与设计虽已标记冻结，但存在三笔已经写进文档规则、却始终没有执行的动作：

1. `ACC-LINK-001` 要求需求与设计、代码、证据双向链接。实际有 306 处引用指向从未签发的
   编号族（`SEC-AUTH-*`、`DATA-ACCOUNT-*`、`INT-API-*`、`NFR-REL-*` 等），
   而落地的族使用另一套命名。按这些回链无法找到上游约束。
2. `02-legacy-document-register.md` 第 10.3 节定义了六步收口，只执行到第 3 步。
   `doc/INDEX.md` 因此长期把旧 API 与数据规范列为“当前权威”，与 v1 真源并列。
3. `ACC-COVER-001` 要求设计阶段开始时建立需求级矩阵。设计已全部冻结，矩阵仍是空的。
   `doc/dev-rules.md` 被三处引用，文件不存在。

这三笔已于本次收口全部处理，并把对应规则变成 `node --test` 中的检查。
根本原因是冻结动作没有可执行的前置检查，仅靠人工记得 —— 已写入 `doc/lessons.md`。

## 4. 人工参与边界

阶段 0 当前没有待用户决定的产品或范围问题。目录、契约骨架、测试夹具、空库检查和自动化门禁，均可根据已冻结需求与设计直接完成。

只有在实现中发现以下情况，才需要重新提交人工决策：

- 会改变用户可见流程或产品行为；
- 会新增旧版本兼容承诺；
- 会与已冻结需求、设计或 `D-V1-012` 冲突。

阶段 0 结束时只汇总门禁结果，不逐文件请求人工审核。

## 5. 当前证据

- 空库初始化测试提交：`4ac336d test: verify v1 clean initialization`
- 人工参与边界澄清提交：`e59ba05 doc: clarify stage zero decision boundary`
- 文档收口的可重复检查：`node tools/doc-check.mjs`（四项）、
  `node tools/build-traceability.mjs --check`（矩阵一致性）
- 当前分支：`codex/v1-rewrite`

## 6. 下一步

直接继续阶段 0 的通用工程工作，优先建立：

1. v1 共享契约目录和版本清单（`ARCH-DEC-02` 的 JSON Schema/OpenAPI 真源）；
2. 匿名课程夹具（不得引用 `SRC-054` 的权利未核验素材）；
3. 契约版本、旧响应/旧存储拒绝和本机数据不上传的自动化证据；
4. 秘密扫描和依赖锁检查，与已建立的文档检查并入同一门禁。

阶段 0 门禁通过后，再进入阶段 1 的服务端身份、工作空间和课程领域实现。
详细工作包顺序见 `doc/plans/v1-code-refactor-execution-plan.md`。
