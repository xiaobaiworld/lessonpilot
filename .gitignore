# Dependencies
node_modules/
backend/.venv/
backend/.pytest_cache/
backend/.coverage
backend/**/__pycache__/
*.py[cod]

# Build output
dist/
build/

# Local config and secrets
.env
.env.*
!.env.example
config.local.*
*.key
*.pem

# Logs
logs/
*.log
backend/*.db
# 备份与 SQLite 副本文件：`*.db` 匹配不到 knownmap.db.bak-<时间戳> 这种名字，
# 而它确实被提交过一次（8f37caf，用 `git add backend` 带进来的）。
backend/*.db.bak*
backend/*.db-*
backend/*.sqlite*

# macOS
.DS_Store

# Agent tool state (not project content)
.superpowers/
.gstack/
.playwright-cli/
output/playwright/

# 临时站点产物
site/
