{
  "name": "@v1/extension",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "v1 Chrome MV3 extension - student learning runtime on Bilibili",
  "manifest_version": 3,
  "engines": {
    "chrome": ">=120"
  },
  "scripts": {
    "build": "vite build",
    "build:local": "KNOWNMAP_TARGET=local vite build",
    "build:production": "KNOWNMAP_TARGET=production vite build",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "vite": "^6.4.3"
  }
}
