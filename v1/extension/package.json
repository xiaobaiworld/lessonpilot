{
  "name": "@v1/extension",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "v1 Chrome MV3 extension - student learning runtime on Bilibili",
  "manifest_version": 3,
  "engines": {
    "chrome": ">=120"
  },
  "scripts": {
    "build": "npm run build:content && npm run build:entries",
    "build:content": "KNOWNMAP_CONTENT_BUILD=1 vite build",
    "build:entries": "KNOWNMAP_CONTENT_BUILD=0 vite build",
    "build:local": "KNOWNMAP_TARGET=local npm run build",
    "build:production": "KNOWNMAP_TARGET=production npm run build",
    "build:all": "npm run build:local && npm run build:production",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "vite": "^6.4.3"
  }
}
